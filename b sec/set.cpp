
#include<bits/stdc++.h>
#include<set>
using namespace std;

int main()
{
    set <int> s[4];
    set<int>::iterator it;       // for print ..
    for(it=s.begin();it!=s.end();it++)
    s.insert[i];
   /* s.insert(7);
    s.insert(-1);
    s.insert(0);
*/
    for(it=s.begin();it!=s.end();it++)
        cout<<*it[i]<<endl;





}
